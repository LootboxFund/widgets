overwrite: true
schema: 'http://localhost:4000/graphql' # NEXT_PUBLIC_GRAPHQL_ENDPOINT
documents: 'src/**/*.graphql'
generates:
  src/lib/graphql/generated/types.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
