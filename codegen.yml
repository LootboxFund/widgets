overwrite: true
schema: 'http://localhost:8080/graphql' # TODO: dynamically load from manifest
generates:
  src/lib/api/graphql/generated/types.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
